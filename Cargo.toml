[package]
authors = ["Takahiro Sato <harehare1110@gmail.com>"]
categories = ["command-line-utilities", "text-processing"]
description = "HTTP server for mq scripts"
edition = "2024"
homepage = "https://mqlang.org/"
keywords = ["markdown", "jq", "query", "http"]
license = "MIT"
name = "mq-http"
readme = "README.md"
repository = "https://github.com/harehare/mq-http"
version = "0.1.4"

[dependencies]
axum = "0.8.8"
futures-util = "0.3"
axum-server = {version = "0.8", features = ["tls-rustls"]}
clap = {version = "4.6.1", features = ["derive", "env"]}
miette = {version ="7.6.0", features = ["fancy"]}
mq-lang = {version = "0.5.29", features = ["cst", "sync", "ast-json"]}
mq-markdown = {version = "0.5.29", features = ["html-to-markdown", "json"]}
notify = "8"
mimalloc = {version = "0.1.48", optional = true}
opentelemetry = "0.31"
opentelemetry-otlp = {version = "0.32", features = ["http-proto", "reqwest-client"]}
opentelemetry_sdk = {version = "0.31", features = ["rt-tokio"]}
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
serde_urlencoded = "0.7"
tokio = {version = "1.52.3", features = ["full"]}
toml = "1.1"
tower-http = {version = "0.6.10", features = ["limit", "trace", "cors", "compression-full", "request-id", "fs"]}
base64 = "0.22"
tracing = {version = "0.1.44"}
tracing-opentelemetry = "0.32"
tracing-subscriber = {version = "0.3.23", features = ["env-filter"]}
yaml-rust2 = "0.11.0"

[dev-dependencies]
http-body-util = "0.1"
rstest = "0.26"

[[bin]]
name = "mq-http"
path = "src/main.rs"

[features]
default = ["std", "use_mimalloc"]
std = []
use_mimalloc = ["mimalloc"]

